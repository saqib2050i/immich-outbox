# Machine-specific: points at wherever the Android SDK happens to live.
local.properties

.gradle/
build/
app/build/
*.apk

# Signing keys. Committing one to a public repository would let anyone
# build an update Android accepts as this app.
*.keystore
*.jks
*.p12
