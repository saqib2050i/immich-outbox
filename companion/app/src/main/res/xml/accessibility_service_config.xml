<?xml version="1.0" encoding="utf-8"?>
<!--
  packageNames restricts this service to Google Photos. It is the difference
  between "an app that can read every screen on the phone" and "an app that
  can read the Google Photos storage screen", and it is why granting the
  accessibility permission here is a much smaller thing than it sounds.

  If a future Android puts the confirmation in a system dialog rather than
  in Photos itself, that package has to be added here. The service simply
  will not see it otherwise.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged|typeWindowContentChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagDefault"
    android:canRetrieveWindowContent="true"
    android:description="@string/service_description"
    android:notificationTimeout="200"
    android:packageNames="com.google.android.apps.photos" />
